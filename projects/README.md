# Argo CD Projects

Project definitions currently live under `bootstrap/projects/` so they are applied before the root application. This directory is reserved for future repository-wide project documentation or definitions.
