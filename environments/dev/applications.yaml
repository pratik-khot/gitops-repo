clusterConfig:
  clusterFullName: REPLACE_WITH_DEV_EKS_CLUSTER_NAME
  environment: dev
  awsRegion: REPLACE_WITH_DEV_AWS_REGION

apps:
  vote-app:
    enabled: true
    installer: kustomize
    path: apps/vote-app/overlays/dev
    project: apps
    destination:
      namespace: vote-app-dev
    argoSyncWave: "50"
    syncOptions:
      - CreateNamespace=true
