apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Add explicit child Argo CD Application manifests here as applications are added.
# Keep workload Kustomize resources in each apps/<app-name>/ overlay.
resources: []
